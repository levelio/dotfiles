#:schema https://yazi-rs.github.io/schemas/yazi.json

[mgr]
ratio = [3, 3, 10]
linemode = "size"

[preview]
max_width = 2000
max_height = 2400

[opener]
edit = [
  { run = "${EDITOR:-vim} %s", desc = "edit", for = "unix", block = true },
]
